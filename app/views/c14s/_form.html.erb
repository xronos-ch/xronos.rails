<%= bootstrap_form_with(model: @c14, local: true, id: "c14Form") do |f| %>
  <% if @c14.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@c14.errors.count, "error") %> prohibited this c14 from being saved:</h2>

      <ul>
        <% @c14.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= render "shared/confirm_merge_warning" if @merge_duplicate %>
  <%= hidden_field_tag :confirm_merge, "true" if @merge_duplicate %>

  <%= render  'fields', f: f %>

  <%= content_for :buttons do %>
    <%= button_tag "Cancel", type: 'reset', class: 'btn btn-danger', form: "c14Form", data: { "bs-dismiss": "modal" } %>
    <%= button_tag "Save", class: 'btn btn-success', form: "c14Form" %>
  <% end %>

<% end %>

