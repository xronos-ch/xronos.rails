<% content_for :title, "#{@c14.lab_identifier}, #{C14.label} from #{@c14.site.name}" %>

<header id="c14-header" class="bg-light text-primary">
  <div class="container my-5">
    <h1 class="display-1"><%= @c14.lab_identifier %></h1>
    <h2 class="display-6">
      <%= "#{C14.label.humanize} from " %>
      <%= link_to @c14.site.name, @c14.site, class: "link-light" %>
    </h2>
  </div>
</header>

<section id="c14-calibration" class="bg-primary">
  <div class="container d-flex justify-content-center p-0" style="margin-bottom: -36px">
    <%= calibrator(@c14) %>
  </div>
</section>

<section id="c14-details" class="bg-body">
  <% if can? :edit, C14 %>
    <div class="container my-5">
      <%= link_to(edit_c14_path, class: "btn btn-sm btn-primary", data: { "turbo_frame": "remote_modal" }) do %>
        <%= fa_icon "pencil", text: "Edit radiocarbon date" %>
      <% end %>
    </div>
  <% end %>

  <div class="container my-5">
    <h2>Measurement details</h2>
    <%= render "c14_measurement", c14: @c14 %>
  </div>

  <div class="container my-5">
    <h2>Context</h2>
    <%= render "c14_context", c14: @c14 %>
  </div>
</section>

<section id="c14-site-details" class="bg-light">
  <div class="container my-5">
    <h2>Site details</h2>
    <%= render 'sites/site_details', :site => @c14.site %>
    <p><%= link_to "More information about #{@c14.site.name}", @c14.site %></p>
  </div>
</section>

<section id="references" class="bg-body">
  <div class="container my-5">
    <h2>Bibliography</h2>
    <%= render "references/bibliography", references: (@c14.references + @c14.site.references).uniq %>
  </div>
</section>

<section id="xronos-metadata" class="bg-body">
  <div class="container my-5">
    <div class="row">

      <div class="col">
        <h2>XRONOS metadata</h2>
        <%= render "shared/record_metadata", record: @c14, record_label: C14.label %>
      </div>

      <div class="col">
        <h2>Revision history</h2>
        <%= render "shared/paper_trail", item: @c14 %>
      </div>
  </div>
</section>
