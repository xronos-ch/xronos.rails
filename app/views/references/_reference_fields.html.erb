<% disabled ||= false  
#We do this so if disabled is not passed to the partial it doesn't crash. 
# We default it to false 
%>

<%= f.text_control :short_ref, label: 'Label', placeholder: 'Libby1955', disabled: disabled %>
<%= f.text_control_multiline :bibtex, label: 'BibTeX', disabled: disabled %>
