<article class="p-3 mb-3 bg-light">
  <h3 class="h4"><%= link_to site.name, site %></h3>
  <p class="mb-0 d-flex align-items-center">
    <%= xr_icon Site, {}, { class: "me-1", style: "height: 1.5rem;" } %>
    <span><%= Site.label.humanize %> in <%= site.country %></span>
  </p>
</article>
