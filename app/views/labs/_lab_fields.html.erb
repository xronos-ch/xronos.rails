<% disabled ||= false  
#We do this so if disabled is not passed to the partial it doesn't crash. 
# We default it to false 
%>

<%= f.text_field :name, placeholder: 'Bern', disabled: disabled %>
<%= f.check_box :active, disabled: disabled %>

