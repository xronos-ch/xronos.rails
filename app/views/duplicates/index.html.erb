<div class="container my-5">

  <h1>Duplicates</h1>

  <p>This page lists all <i>exact</i> duplicates, by model. It probably doesn't have long to live...</p>

  <h2>Sites (<%= @pagy.count %>)</h2>

  <% @site_dupes.each do |sites| %>
    <%= render "sites/table", sites: sites %>
  <% end %>

  <%== pagy_bootstrap_nav(@pagy) %>

</div>
