<%= form_with(model: ecochronological_unit, local: true) do |form| %>
  <% if ecochronological_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ecochronological_unit.errors.count, "error") %> prohibited this ecochronological_unit from being saved:</h2>

      <ul>
      <% ecochronological_unit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :approx_start_time %>
    <%= form.number_field :approx_start_time %>
  </div>

  <div class="field">
    <%= form.label :approx_end_time %>
    <%= form.number_field :approx_end_time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
