<%= bootstrap_form_with(model: [:admin, @article], local: true, data: { controller: "article-editor" }) do |f| %>
  <%= render 'fields', f: f %>
  <%= f.submit "Save", class: "btn btn-primary" %>
<% end %>
