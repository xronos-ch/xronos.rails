<div class="container">
  <%= render 'shared/action_breadcrumbs', i: @country %>
  <%= render 'shared/notice' %>

  <h1><%= @country.name %></h1>

  <%= render 'shared/action_buttons', i: @country %>
</div>
