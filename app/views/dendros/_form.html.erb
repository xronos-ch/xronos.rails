<%= bootstrap_form_with(model: @dendro, local: true, id: "dendroForm") do |f| %>
  <% if @dendro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dendro.errors.count, "error") %> prohibited this dendro from being saved:</h2>

      <ul>
        <% @dendro.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= render  'fields', f: f %>

  <%= content_for :buttons do %>
    <%= button_tag "Cancel", type: 'reset', class: 'btn btn-danger', form: "dendroForm", data: { "bs-dismiss": "modal" } %>
    <%= button_tag "Save", class: 'btn btn-success', form: "dendroForm" %>
  <% end %>

<% end %>

